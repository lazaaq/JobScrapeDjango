# Django environment
DEBUG=False
SECRET_KEY=replace-with-a-long-random-secret
ALLOWED_HOSTS=jobscrape.example.com,127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=https://jobscrape.example.com

# Optional: if you want to run behind HTTPS reverse proxy
# SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https
